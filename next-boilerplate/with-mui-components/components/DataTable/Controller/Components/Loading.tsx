import React from 'react';
import LoadingIndicator from './LoadingIndicator';

import { LoaderRoot } from '../style';

const Loading = () => (
	<LoaderRoot>
		<LoadingIndicator />
	</LoaderRoot>
);

export default Loading;
